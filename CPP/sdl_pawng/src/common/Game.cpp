#include "Game.hpp"

Game::Game()
{
	;
}

void Game::predictInput(const std::string&)
{
	;
}
/*
void moveActors()
{
    switch (keydown) {
    case 1:
        if (clientPlayer.gety() - 8 > -1) {
            clientPlayer.sety(clientPlayer.gety() - 8);
        }
        break;
    case 2:
        if (clientPlayer.gety() + 48 < 480) {
            clientPlayer.sety(clientPlayer.gety() + 8);
        }
    }
}

update()
*/
